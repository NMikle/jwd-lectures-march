plugins {
    id 'application'
}

application {
    mainClassName = 'com.epam.jwd.app.Application'
}

group 'com.epam.jwd.space'
version '1.0-SNAPSHOT'

dependencies {
    implementation project(':spaceships-api')
    runtimeOnly project(':spaceships-in-database')
}